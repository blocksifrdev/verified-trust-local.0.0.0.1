{
  "name": "@blocksifr/verifiedtrust-local",
  "version": "0.1.0",
  "description": "Open-source, local-first non-human identity authority graph and execution inflection scanner",
  "license": "Apache-2.0",
  "bin": {
    "verifiedtrust": "./dist/cli/index.js"
  },
  "main": "./dist/cli/index.js",
  "files": [
    "dist/",
    "docs/",
    "examples/",
    "action.yml",
    "Dockerfile",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "commander": "^12.0.0",
    "csv-parse": "^5.5.0",
    "glob": "^10.3.0",
    "js-yaml": "^4.1.0",
    "ora": "^8.0.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
